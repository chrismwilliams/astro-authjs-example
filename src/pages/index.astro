---
// import { db, Session, User } from "astro:db";
import Layout from "@/layouts/Layout.astro";
import AstroIcon from "@/components/AstroIcon.astro";

const title = "Astro and Lucia";

// const sessions = await db.select().from(Session);
// const users = await db.select().from(User);

// console.table(sessions);
// console.table(users);
---

<Layout title={title}>
	<main class="flex flex-1 flex-col items-center justify-center gap-4 p-4">
		<h1 class="text-center text-2xl font-bold sm:text-4xl">{title}</h1>
		<ul class="mt-6 flex items-center gap-6 text-2xl font-bold">
			<li
				class="flex h-20 w-20 items-center justify-center rounded-lg border-t border-transparent bg-white shadow dark:border-zinc-700 dark:bg-zinc-800 dark:shadow-none"
			>
				<AstroIcon />
			</li>
		</ul>
		<p class="mt-6 max-w-prose">This page is SSR</p>
	</main>
</Layout>
